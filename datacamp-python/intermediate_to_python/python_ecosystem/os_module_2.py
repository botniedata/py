# Import the os module
import os

# Get the current working directory - cwd means `current working directory`
print("Current working directory:", os.getcwd())

# Check the environment variables - no parethesis because its an attribute
print("Environment variables:", os.environ)
